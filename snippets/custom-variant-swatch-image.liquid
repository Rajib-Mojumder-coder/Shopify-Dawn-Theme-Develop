{% liquid
  assign swatch_image = blank
  for variant in product.variants
    if variant.featured_media and variant.options contains swatch_value
      assign swatch_image = variant.featured_media.preview_image
      break
    endif
  endfor
%}